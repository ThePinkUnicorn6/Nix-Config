{ config, lib, pkgs, ... }:

{
  programs.emacs = {
    extraPackages = epkgs: with epkgs; [
      lsp-mode
      lsp-ui
      
      nix-mode
      rust-mode
    ];

    extraConfig = ''
    
    '';
  };  
}
